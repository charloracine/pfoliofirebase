import { Container } from "@material-ui/core";
import React from "react";
import FormProjects from "../components/Form/FormProjects";

const AddProject = () => {
  return (
    <Container>
      <FormProjects />
    </Container>
  );
};

export default AddProject;
